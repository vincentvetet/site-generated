"use client"; import Link from "next/link"; import { authClient } from "@/lib/auth-client"; import { Button } from "@/components/ui/button"; import { useState } from "react";
export default function Register(){const [error,setError]=useState("");async function submit(f:FormData){const {error}=await authClient.signUp.email({name:String(f.get('name')),email:String(f.get('email')),password:String(f.get('password')),callbackURL:'/dashboard'});if(error)setError(error.message)}return <main className="grid min-h-screen place-items-center p-5"><form action={submit} className="w-full max-w-md rounded-2xl border bg-surface p-7"><Link href="/" className="text-sm font-bold text-violet-300">← Site Generated</Link><h1 className="mt-6 text-3xl font-bold">Créer un compte</h1>{error&&<p className="mt-4 rounded bg-red-500/10 p-3 text-sm text-red-300">{error}</p>}<label className="mt-5 block text-sm">Nom<input required name="name" className="mt-2 w-full rounded-lg border bg-black/20 p-3"/></label><label className="mt-4 block text-sm">Email<input required name="email" type="email" className="mt-2 w-full rounded-lg border bg-black/20 p-3"/></label><label className="mt-4 block text-sm">Mot de passe<input required minLength={8} name="password" type="password" className="mt-2 w-full rounded-lg border bg-black/20 p-3"/></label><Button className="mt-6 w-full" type="submit">Créer mon compte</Button><p className="mt-6 text-center text-sm text-zinc-400">Déjà inscrit ? <Link className="text-violet-300" href="/login">Connexion</Link></p></form></main>}
