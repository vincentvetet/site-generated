"use client"; import { useRouter } from "next/navigation"; import { Button } from "@/components/ui/button"; import { useState } from "react";
export default function NewShop(){const router=useRouter(),[error,setError]=useState('');async function save(f:FormData){const res=await fetch('/api/shops',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({name:f.get('name'),description:f.get('description'),logo:f.get('logo'),rating:f.get('rating')})});if(!res.ok)return setError('Impossible de créer cette boutique. Vérifiez vos droits administrateur.');router.push('/shops')}return <main className="mx-auto max-w-xl p-8"><h1 className="text-3xl font-bold">Nouvelle boutique</h1><form action={save} className="mt-7 space-y-4 rounded-xl border bg-surface p-6">{error&&<p className="text-sm text-red-300">{error}</p>}<input required name="name" placeholder="Nom" className="w-full rounded-lg border bg-black/20 p-3"/><input name="logo" type="url" placeholder="URL du logo (optionnel)" className="w-full rounded-lg border bg-black/20 p-3"/><textarea required name="description" minLength={10} placeholder="Description" className="min-h-28 w-full rounded-lg border bg-black/20 p-3"/><input name="rating" type="number" min="0" max="5" step=".1" placeholder="Note / 5" className="w-full rounded-lg border bg-black/20 p-3"/><Button type="submit">Enregistrer</Button></form></main>}
