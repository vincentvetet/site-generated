import { Navbar } from "@/components/navbar"; import { Footer } from "@/components/footer"; import { prisma } from "@/lib/prisma"; import Link from "next/link"; import { Button } from "@/components/ui/button";
export default async function Shops({searchParams}:{searchParams:Promise<{q?:string}>}){const {q=''}=await searchParams;const shops=await prisma.shop.findMany({where:q?{name:{contains:q,mode:'insensitive'}}:undefined,orderBy:{rating:'desc'}});return <><Navbar/><main className="mx-auto min-h-[calc(100vh-142px)] max-w-6xl px-5 py-12"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-sm text-violet-300">● Répertoire vérifié</p><h1 className="mt-2 text-4xl font-bold">Boutiques</h1></div><Link href="/shops/new"><Button>+ Ajouter une boutique</Button></Link></div><form className="mt-7"><input name="q" defaultValue={q} placeholder="Rechercher une boutique..." className="w-full max-w-md rounded-lg border bg-surface px-4 py-3 text-sm outline-none focus:border-violet-400"/></form><section className="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{shops.map(s=><article className="rounded-xl border bg-surface p-5" key={s.id}><div className="flex items-center justify-between"><div className="grid h-10 w-10 place-items-center rounded-lg bg-violet-500/15 text-violet-300">{s.name[0]}</div><span className="rounded bg-emerald-500/10 px-2 py-1 text-xs text-emerald-300">{s.status}</span></div><h2 className="mt-5 font-semibold">{s.name}</h2><p className="mt-2 text-sm text-zinc-400">{s.description}</p><p className="mt-5 text-xs text-violet-300">★ {s.rating.toFixed(1)} / 5</p></article>)}{shops.length===0&&<p className="text-zinc-400">Aucune boutique trouvée.</p>}</section></main><Footer/></>}
