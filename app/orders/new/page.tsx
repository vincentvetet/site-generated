"use client"; import { useRouter } from "next/navigation"; import { Button } from "@/components/ui/button"; import { useState } from "react";
export default function NewOrder(){const router=useRouter(),[error,setError]=useState('');async function save(f:FormData){const res=await fetch('/api/orders',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({shopId:f.get('shopId'),price:f.get('price'),tracking:f.get('tracking'),status:'CONFIRMED'})});if(!res.ok)return setError('Impossible de créer la commande. Utilisez un identifiant de boutique valide.');router.push('/orders')}return <main className="mx-auto max-w-xl p-8"><h1 className="text-3xl font-bold">Nouvelle commande</h1><form action={save} className="mt-7 space-y-4 rounded-xl border bg-surface p-6">{error&&<p className="text-sm text-red-300">{error}</p>}<input required name="shopId" placeholder="Identifiant de la boutique" className="w-full rounded-lg border bg-black/20 p-3"/><input required name="price" type="number" min=".01" step=".01" placeholder="Prix" className="w-full rounded-lg border bg-black/20 p-3"/><input name="tracking" placeholder="Numéro de suivi (optionnel)" className="w-full rounded-lg border bg-black/20 p-3"/><Button type="submit">Créer la commande</Button></form></main>}
